<?xml version="1.0" encoding="utf-8" ?>

<ContentView
    x:Class="Sparky.TrakApp.Controls.Common.FullPageErrorControl"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="FullPageErrorControlView"
    mc:Ignorable="d">
    <ContentView.Content>
        <!--  Full-page label telling the user that the request returned no results without error.  -->
        <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
            <Label Text="{Binding ErrorMessage, Source={x:Reference FullPageErrorControlView}}" />
            <Button
                Command="{Binding RefreshCommand, Source={x:Reference FullPageErrorControlView}}"
                IsVisible="{Binding IsRefreshable, Source={x:Reference FullPageErrorControlView}}"
                Text="Refresh" />
        </StackLayout>
    </ContentView.Content>
</ContentView>